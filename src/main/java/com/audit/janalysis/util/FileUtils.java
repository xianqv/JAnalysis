package com.audit.janalysis.util;

import org.springframework.web.multipart.MultipartFile;


import java.io.IOException;

public class FileUtils {

    public  void  restoreFile(MultipartFile multipartFile) throws IOException {
        //InputStream inputStream = new FileInputStream(multipartFile.getOriginalFilename());

    }
}